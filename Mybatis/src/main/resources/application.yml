spring:
  application:
    name: Emp_1ProjectMybatis

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/study?serverTimezone=UTC
    username: root
    password: 1004

# mybatis
mybatis:
  mapper-locations: classpath:/mapper/*.xml
